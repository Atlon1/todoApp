import React from 'react';

const Task = ({task, handleDeleteTask} : {task: any, handleDeleteTask: (id: number) => void}) => {
    return (
        <div>
            Task
        </div>
    );
};

export default Task;